# Legado / rascunho

Material de estudo e exploração inicial. **Não faz parte da solução final** —
mantido só como referência.

A solução entregue está em `src/mflix_ingest/`, `jobs/`, `notebooks/` e `config/`.

| Arquivo | O que era |
|---|---|
| `mongo_reader.py` | primeira versão da classe de leitura do Mongo (`MongoReader`) — evoluiu para `src/mflix_ingest/mongo_source.py` |
| `mongo_reader_.py` | variação do anterior com exemplos de uso |
| `Exercicios.py` | exercícios de exploração de schema/tipos com `movies` |
